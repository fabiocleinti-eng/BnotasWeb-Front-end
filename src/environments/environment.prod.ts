// Ambiente de PRODUÇÃO — usado automaticamente no `ng build` (configuração production).
// Troque a apiUrl pela URL pública da sua API quando fizer o deploy (ex.: https://api.seudominio.com.br/api)
export const environment = {
  production: true,
  apiUrl: 'https://TROQUE-PELA-URL-DA-SUA-API/api'
};
